# Lexer Tests

Foundation ASM lexer coverage currently lives in `tests/run_all.sh` through the
invalid string, invalid escape, invalid character path, and hello source scan.
Dedicated token-dump fixtures are a next step.

