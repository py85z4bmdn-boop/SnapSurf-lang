fn accept_wrapped value u8 -> i32
    if value == 0 ->
        ret 0
    else ->
        ret 1
    end
end

fn main -> i32
    let max u8 = 255
    let one u8 = 1
    ret accept_wrapped(max + one)
end
