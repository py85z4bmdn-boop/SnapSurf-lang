fn main -> i32
    let a u64 = 10
    let b u64 = 5
    let c u64 = 3
    let d u64 = 2
    let sum u64 = a + b
    let diff u64 = sum - c
    let product u64 = diff * d
    if product == 24 ->
        ret 0
    else ->
        ret 1
    end
end
