# Parser Tests

Foundation ASM parser coverage currently lives in `tests/run_all.sh` through
hello parsing, missing `end`, invalid main signature, and return validation.
Dedicated parser fixtures are a next step.

